#include<iostream>
#include<algorithm>

using namespace std;

/*

Let us understand the problem 

Cycle sort is
       in place
       Optimal writes(Each value is written 0 times if in correct postion and 1 time to put it in
       correct position if not present)

An array to be sorted can be divided into cycles,
Cycles can be visualized as a graph- We have n nodes and an edge directed from node i to node j if the i-th element index
must be present at the j-th index in the sorted array













*/







